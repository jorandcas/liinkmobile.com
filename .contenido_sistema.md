# 📘 DOCUMENTACIÓN COMPLETA DEL SISTEMA DN VERIFICATION

> **Este archivo contiene toda la información sobre el sistema. Consulta este archivo primero antes de preguntar.**

---

## 📋 RESUMEN EJECUTIVO

**Nombre del Sistema:** DN (Distribuidor Number) Verification System - Multitenant Edition para Movistar

**Versión:** 2.0.0 (MVP Multitenant)

**Propósito:** Validar números de teléfono contra el API de inscripción de distribuidores de Movistar con arquitectura multitenant completa

**Arquitectura:** Sistema multitenant con bases de datos PostgreSQL separadas por tenant para máximo aislamiento

**Funcionalidades Principales:**
- ✅ **Sistema Multitenant completo** con BDs separadas por tenant
- ✅ **Validación individual de teléfonos**
- ✅ **Validación por lotes (JSON)**
- ✅ **Validación masiva desde CSV** con progreso en tiempo real (SSE)
- ✅ **Gestión de campañas de validación**
- ✅ **Panel web con interfaz de usuario**
- ✅ **Autenticación JWT profesional** con jsonwebtoken
- ✅ **Autorización por roles** (SuperAdmin, Tenant Admin)
- ✅ **Cifrado AES-256-GCM** para API Keys
- ✅ **Sistema de auditoría completo**
- ✅ **Cambio obligatorio de contraseña** en primer login
- ✅ **Bloqueo de cuentas** por intentos fallidos
- ✅ **UI SuperAdmin** para gestión de tenants
- ✅ **Landing page de marketing** con integración WhatsApp
- ✅ **Validación automática de API Keys** al crear tenant

---

## 🏗️ ARQUITECTURA DEL PROYECTO

```
DN Verification/
├── src/                          # Código fuente TypeScript
│   ├── client/                   # Cliente HTTP con reintentos
│   │   └── api-client.ts        # Cliente axios reusable
│   ├── config/                   # Configuraciones
│   │   └── endpoints.config.ts   # Endpoints del API
│   ├── controllers/              # Manejadores de peticiones
│   │   ├── auth.controller.ts    # Login, verify token, change password
│   │   ├── campana.controller.ts # CRUD campañas
│   │   ├── distribuidor.controller.ts # Validaciones
│   │   └── superadmin.controller.ts # Gestión de tenants
│   ├── middleware/               # Middleware Express
│   │   ├── auth.middleware.ts    # Verificación JWT
│   │   ├── password-change.middleware.ts # Forzar cambio de contraseña
│   │   └── role.middleware.ts    # Verificación de roles
│   ├── routes/                   # Rutas del API
│   │   ├── auth.routes.ts        # Rutas de autenticación
│   │   ├── campana.routes.ts     # Rutas de campañas
│   │   ├── config.routes.ts      # Configuración del sistema
│   │   ├── distribuidor.routes.ts # Rutas de validación
│   │   └── superadmin.routes.ts  # Rutas de superadmin
│   ├── services/                 # Lógica de negocio
│   │   ├── auth.service.ts       # Autenticación JWT + bcrypt
│   │   ├── audit.service.ts      # Auditoría de acciones
│   │   ├── campana.service.ts    # Gestión campañas
│   │   ├── config.service.ts     # Configuración y distribuidor
│   │   ├── distribuidor.service.ts # Validaciones
│   │   └── tenant.service.ts     # Gestión de tenants (BDs)
│   ├── types/                    # Definiciones TypeScript
│   │   ├── auth.types.ts
│   │   ├── campana.types.ts
│   │   └── distribuidor.types.ts
│   ├── utils/                    # Utilidades
│   │   └── encryption.util.ts    # Cifrado AES-256-GCM
│   └── index.ts                  # Punto de entrada
│
├── public/                       # Archivos web estáticos
│   ├── css/                      # Estilos Tailwind
│   │   ├── main.css             # Input Tailwind
│   │   └── output.css           # CSS compilado
│   ├── js/                       # JavaScript frontend
│   │   ├── login.js             # Lógica login
│   │   ├── dashboard.js         # Panel principal (936 líneas)
│   │   ├── superadmin.js        # Panel SuperAdmin
│   │   └── cambiar-password.js  # Cambio de contraseña
│   ├── login.html                # Página login
│   ├── dashboard.html            # Panel principal
│   ├── superadmin.html           # Panel SuperAdmin
│   └── cambiar-password.html     # Cambio de contraseña
│
├── db/                           # Base de datos
│   ├── init.sql                  # Schema SQL principal
│   └── seed-superadmin.ts        # Crear usuario SuperAdmin
│
├── page/                         # Landing page de marketing
│   ├── index.html               # Página web de presentación
│   ├── PRESENTACION_COTIZACION.md # Documento de ventas
│   └── README.md                # Guía de la landing page
│
├── uploads/                      # Archivos temporales CSV
├── dist/                         # JavaScript compilado (generado)
│
├── .env                          # Variables de entorno (NO en repo)
├── .env.example                 # Template de variables
├── package.json                 # Dependencias npm
├── tsconfig.json                # Configuración TypeScript
├── tailwind.config.js           # Configuración Tailwind
├── postcss.config.js            # Configuración PostCSS
├── TESTING_GUIDE.md             # Guía de testing del MVP
├── diagnostico.js               # Herramienta de diagnóstico
├── check-*.js                   # Scripts de verificación
└── README.md                    # Documentación principal
```

---

## 🔧 STACK TECNOLÓGICO

### Backend
- **Runtime:** Node.js (v20+ recomendado)
- **Lenguaje:** TypeScript 5.3.2 (strict mode)
- **Framework:** Express 4.18.2
- **Base de Datos:** PostgreSQL (pg 8.21.0)
- **Autenticación:** jsonwebtoken 9.0.3 + bcrypt 6.0.0
- **Cifrado:** AES-256-GCM (crypto nativo Node.js)
- **Cliente HTTP:** Axios 1.6.0
- **Validación:** Zod 3.22.4
- **Upload:** Multer 1.4.5-lts.1
- **CSV:** csv-parser 3.0.0
- **Environment:** dotenv 16.3.1
- **CORS:** cors 2.8.5

### Frontend
- **Estilos:** Tailwind CSS 4.2.2
- **JavaScript:** Puro (Vanilla JS - sin framework)
- **PostCSS:** 8.5.8
- **Autoprefixer:** 10.4.27

### Desarrollo
- **Compilador:** tsc (TypeScript)
- **Dev Server:** tsx 4.6.0 (watch mode)
- **Linting:** ESLint 8.54.0

---

## 🔑 VARIABLES DE ENTORNO (.env)

```bash
# ============================================
# SERVIDOR
# ============================================
PORT=3000
NODE_ENV=development

# ============================================
# BASE DE DATOS PRINCIPAL (SuperAdmin)
# ============================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password_postgres
DB_NAME=bd_superadmin

# ============================================
# CIFRADO DE API KEYS - MUY IMPORTANTE
# ============================================
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Debe ser exactamente 32 bytes (44 caracteres en base64)
ENCRYPTION_KEY=tu_clave_base64_44_caracteres_aqui

# ============================================
# JWT
# ============================================
JWT_SECRET=tu_jwt_secreto_muy_largo_y_seguro
JWT_EXPIRATION=24h

# ============================================
# VALIDACIÓN DE API KEYS
# ============================================
VALIDATION_TEST_PHONE=9233250673

# ============================================
# SEGURIDAD DE CONTRASEÑAS
# ============================================
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCK_TIME=900
PASSWORD_MIN_LENGTH=8
```

**Importante:**
- `ENCRYPTION_KEY` es obligatoria para cifrar API Keys (32 bytes en base64)
- `JWT_SECRET` debe ser muy segura para tokens JWT
- `VALIDATION_TEST_PHONE` se usa para validar API Keys antes de crear tenants

---

## 🗄️ ARQUITECTURA DE BASE DE DATOS

### Multitenancy con BDs Separadas

**Estrategia:** Máximo aislamiento con bases de datos PostgreSQL separadas por tenant

```
PostgreSQL Server
├── bd_superadmin          # BD principal
│   ├── tenants            # Tabla de tenants + superadmin
│   └── audit_logs         # Auditoría global
│
├── tenant_govi            # BD del tenant Govi
│   └── validaciones       # Validaciones de Govi
│
├── tenant_mundoelite      # BD del tenant Mundoelite
│   └── validaciones       # Validaciones de Mundoelite
│
└── tenant_{nombre}        # BD de cada tenant
    └── validaciones       # Validaciones del tenant
```

### Tabla Principal: `tenants`

```sql
CREATE TABLE tenants (
  id SERIAL PRIMARY KEY,
  nombre VARCHAR(100) UNIQUE NOT NULL,
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  bd_name VARCHAR(100) UNIQUE NOT NULL,
  api_key_encrypted TEXT,                    -- NULL para superadmin
  api_status VARCHAR(20) DEFAULT 'pendiente', -- 'pendiente' | 'valida' | 'invalida'
  tenant_status VARCHAR(20) DEFAULT 'activo',  -- 'activo' | 'suspendido'
  role VARCHAR(20) DEFAULT 'tenant_admin',     -- 'superadmin' | 'tenant_admin'
  must_change_password BOOLEAN DEFAULT true,   -- Cambio obligatorio al primer login
  last_login_at TIMESTAMP,
  failed_login_attempts INTEGER DEFAULT 0,
  locked_until TIMESTAMP NULL,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

### Tabla de Auditoría: `audit_logs`

```sql
CREATE TABLE audit_logs (
  id SERIAL PRIMARY KEY,
  tenant_id INTEGER REFERENCES tenants(id),
  user_email VARCHAR(255) NOT NULL,
  action VARCHAR(100) NOT NULL,
  details JSONB,
  ip_address VARCHAR(45),
  created_at TIMESTAMP DEFAULT NOW()
);
```

### Tablas de Tenant: `validaciones`

Cada tenant tiene su propia BD con tabla de validaciones:

```sql
CREATE TABLE validaciones (
  id SERIAL PRIMARY KEY,
  telefono VARCHAR(20) NOT NULL,
  resultado JSONB NOT NULL,
  origen VARCHAR(10) NOT NULL,
  exitoso BOOLEAN NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

---

## 🌐 ENDPOINTS EXTERNOS (MOVISTAR)

### Ambiente PROD
```
URL: http://94.74.77.50:8010/api/v1/distribuidores/enrolamiento/{telefono}
Method: GET
Headers:
  - ConsumerName: Movistar
  - Content-Type: application/json
  - x-api-key: {API_KEY_DEL_TENANT}
```

**Nota:** El sistema valida automáticamente las API Keys usando este endpoint antes de crear un tenant

---

## 📡 ENDPOINTS DEL API INTERNO

### Autenticación (`/api/auth`)

#### POST `/api/auth/login`
**Descripción:** Inicia sesión y obtiene token JWT (Universal para SuperAdmin y Tenants)

**Body:**
```json
{
  "email": "admin@tudominio.com",
  "password": "SuperPassword123!"
}
```

**Response:**
```json
{
  "exito": true,
  "token": "eyJhbGciOiJ...",
  "user": {
    "id": 1,
    "nombre": "SuperAdmin",
    "email": "admin@tudominio.com",
    "role": "superadmin",
    "tenant_status": "activo",
    "must_change_password": false
  }
}
```

#### GET `/api/auth/me`
**Descripción:** Obtiene información del usuario actual
**Headers:** `Authorization: Bearer {token}`

#### POST `/api/auth/change-password`
**Descripción:** Cambiar contraseña (requiere contraseña actual)
**Headers:** `Authorization: Bearer {token}`

**Body:**
```json
{
  "currentPassword": "PasswordAnterior123!",
  "newPassword": "PasswordNueva456!"
}
```

---

### SuperAdmin (`/api/superadmin`)

🔒 **Requiere rol:** `superadmin`

#### POST `/api/superadmin/tenants`
**Descripción:** Crear nuevo tenant

**Body:**
```json
{
  "nombre": "Govi",
  "email": "govi@gmail.com",
  "password": "Govi12345678",
  "apiKey": "sk-govi-bc62debea3abf9272efc2c91402f5d2abaffb53df26abd6c"
}
```

**Validaciones:**
- ✅ Valida formato de email
- ✅ Valida longitud mínima de contraseña (8 caracteres)
- ✅ Valida API Key contra endpoint de DN
- ✅ Crea BD separada para el tenant
- ✅ Cifra API Key con AES-256-GCM
- ✅ Hashea contraseña con bcrypt
- ✅ Registra en auditoría

**Response:**
```json
{
  "exito": true,
  "tenant": {
    "id": 2,
    "nombre": "Govi",
    "email": "govi@gmail.com",
    "bdName": "tenant_govi",
    "apiStatus": "valida",
    "tenantStatus": "activo",
    "role": "tenant_admin",
    "mustChangePassword": true,
    "createdAt": "2026-05-19T12:00:00.000Z"
  },
  "mensaje": "Tenant creado exitosamente"
}
```

#### GET `/api/superadmin/tenants`
**Descripción:** Listar todos los tenants

**Response:**
```json
{
  "exito": true,
  "tenants": [
    {
      "id": 2,
      "nombre": "Govi",
      "email": "govi@gmail.com",
      "bdName": "tenant_govi",
      "apiStatus": "valida",
      "tenantStatus": "activo",
      "lastLoginAt": null,
      "createdAt": "2026-05-19T12:00:00.000Z"
    }
  ]
}
```

#### PATCH `/api/superadmin/tenants/:id/suspend`
**Descripción:** Suspender tenant (no puede hacer login)

#### PATCH `/api/superadmin/tenants/:id/activate`
**Descripción:** Activar tenant (reactiva login)

#### GET `/api/superadmin/audit-logs`
**Descripción:** Obtener logs de auditoría
**Query params:** `?limit=100&offset=0&action=tenant_created`

**Response:**
```json
{
  "exito": true,
  "logs": [
    {
      "id": 1,
      "user_email": "admin@tudominio.com",
      "action": "tenant_created",
      "details": {
        "tenantId": 2,
        "tenantName": "Govi",
        "tenantEmail": "govi@gmail.com",
        "bdName": "tenant_govi"
      },
      "ip_address": "127.0.0.1",
      "created_at": "2026-05-19T12:00:00.000Z"
    }
  ]
}
```

#### GET `/api/superadmin/audit-stats`
**Descripción:** Obtener estadísticas de auditoría

---

### Validación (`/api`)

🔒 **Requiere autenticación:** Tenant JWT token

#### POST `/api/validate/single`
**Descripción:** Valida un teléfono individual

**Headers:** `Authorization: Bearer {tenant_token}`

**Body:**
```json
{
  "telefono": "9233250673",
  "verificarEn": ["PROD"]
}
```

**Response:**
```json
{
  "exito": true,
  "telefono": "9233250673",
  "resultados": [
    {
      "telefono": "9233250673",
      "exitoso": true,
      "datos": {
        "dn": "9233250673",
        "enrolado": false
      },
      "origen": "PROD"
    }
  ]
}
```

**Nota:** Usa la API Key del tenant (descifrada) para hacer la petición

#### POST `/api/validate/batch`
**Descripción:** Valida un lote de teléfonos desde JSON

**Body:**
```json
{
  "telefonos": ["9233250673", "9233250674", "9233250675"],
  "verificarEn": ["PROD"]
}
```

#### POST `/api/validate/bulk`
**Descripción:** Validación masiva desde CSV con SSE

**Content-Type:** `multipart/form-data`

**Form Data:**
- `file`: archivo CSV
- `verificarEn`: ["PROD"] (stringified JSON)

**Response:** Server-Sent Events (SSE) stream
```
data: {"type":"progress","procesados":10,"total":100,"porcentaje":10}

data: {"type":"complete","data":{...}}
```

#### GET `/api/health`
**Descripción:** Health check del servidor

---

### Campañas (`/api/campanas`)

🔒 **Requiere autenticación:** Tenant JWT token

#### POST `/api/campanas`
**Descripción:** Crea una nueva campaña

**Body:**
```json
{
  "nombre": "Campaña 1",
  "tipo": "multiple",
  "entorno": "AMBOS",
  "resultados": [...]
}
```

#### GET `/api/campanas`
**Descripción:** Obtiene todas las campañas del tenant actual

#### GET `/api/campanas/:id`
**Descripción:** Obtiene una campaña por ID

#### DELETE `/api/campanas/:id`
**Descripción:** Elimina una campaña

---

### Configuración del Sistema (`/api/config`)

#### GET `/api/config`
**Descripción:** Obtiene la configuración del sistema

**Response:**
```json
{
  "exito": true,
  "config": {
    "ambiente": "development",
    "version": "2.0.0"
  }
}
```

---

## 🔐 SEGURIDAD

### Implementación Actual (PRODUCCIÓN-READY)

#### ✅ Autenticación
- **JWT profesional** usando librería `jsonwebtoken`
- **Tokens de 24 horas** de validez
- **Almacenamiento seguro** en localStorage (frontend)

#### ✅ Autorización
- **Roles:** `superadmin` y `tenant_admin`
- **Middleware de verificación** de roles
- **Rutas protegidas** por middleware

#### ✅ Contraseñas
- **Hashing bcrypt** con 10 salt rounds
- **Validación de longitud** mínima (8 caracteres configurable)
- **Cambio obligatorio** en primer login
- **Verificación de contraseña actual** para cambiarla

#### ✅ Protección contra Ataques
- **Bloqueo de cuenta** después de 5 intentos fallidos (configurable)
- **Tiempo de bloqueo** 15 minutos (900 segundos, configurable)
- **Limitación de intentos** almacenada en BD

#### ✅ Cifrado de Datos Sensibles
- **AES-256-GCM** para API Keys
- **IV único** por cada cifrado (12 bytes)
- **AuthTag** para verificación de integridad
- **Formato:** `iv:authTag:encrypted`
- **Clave de 32 bytes** generada con crypto.randomBytes()

#### ✅ Auditoría
- **Registro de acciones críticas:** tenant_created, tenant_suspended, login_success, login_failed
- **Detalles en JSONB** para flexibilidad
- **IP address** tracking
- **Timestamps** en todas las operaciones

#### ⚠️ Limitaciones de Seguridad (Mejoras Futuras)
- [ ] Rate limiting por IP
- [ ] CSRF protection
- [ ] Helmet.js para headers seguros
- [ ] HTTPS obligatorio (configuración de servidor)
- [ ] Cookies httpOnly y secure para tokens
- [ ] Sanitización de inputs contra XSS
- [ ] 2FA (Two-Factor Authentication)

---

## 🔄 FLUJO DE DATOS

### Flujo de Creación de Tenant (SuperAdmin)
```
1. SuperAdmin login → JWT token
2. POST /api/superadmin/tenants con datos del tenant
3. SuperAdminController.createTenant()
4. TenantService.createTenant():
   a. Validar nombre único
   b. Validar email único
   c. Validar API Key (test request a DN)
   d. Crear BD separada para tenant (tenant_{nombre})
   e. Crear tabla validaciones en BD del tenant
   f. Cifrar API Key con AES-256-GCM
   g. Hashear contraseña con bcrypt
   h. Insertar tenant en BD superadmin
5. Registrar en audit_logs
6. Retornar tenant creado (sin API Key descifrada)
```

### Flujo de Login (Universal)
```
1. Usuario ingresa email/password
2. POST /api/auth/login
3. AuthService.login():
   a. Buscar usuario por email en BD
   b. Verificar si está activo (tenant_status)
   c. Verificar si no está bloqueado (locked_until)
   d. Verificar contraseña con bcrypt.compare()
   e. Si falla:
      - Incrementar failed_login_attempts
      - Si >= 5, bloquear cuenta (locked_until = now + 15min)
      - Retornar error
   f. Si éxito:
      - Resetear failed_login_attempts a 0
      - Actualizar last_login_at
      - Generar JWT token con userId, email, role
4. Retornar token + datos del usuario (sin password_hash)
5. Cliente guarda token en localStorage
6. Requests subsiguientes incluyen header: Authorization: Bearer {token}
```

### Flujo de Validación de Teléfonos (Tenant)
```
1. Tenant login → JWT token
2. POST /api/validate/single con teléfono
3. authMiddleware verifica token → Extrae tenant_id del JWT
4. DistribuidorController.validarIndividual()
5. DistribuidorService.validarMultipleAmbientes():
   a. Obtener API Key descifrada del tenant (getTenantApiKey)
   b. Por cada ambiente seleccionado:
      - ApiClient.get() con API Key del tenant
      - 5 segundos de delay entre ambientes
   c. Guardar resultados en BD del tenant (tenant_{nombre})
6. Resultados retornados al cliente
7. Opción de guardar como campaña en BD del tenant
```

### Flujo de Validación Masiva CSV (Tenant)
```
1. Tenant login → JWT token
2. Usuario sube archivo CSV
3. Multer guarda archivo en uploads/
4. POST /api/validate/bulk con FormData
5. DistribuidorController.validarMasiva()
6. DistribuidorService.validarMasiva():
   a. Leer CSV con csv-parser
   b. Abrir canal SSE con cliente
   c. Por cada teléfono:
      - Validar en ambientes seleccionados
      - 5 segundos delay entre requests
      - Enviar progreso via SSE
   d. Al completar:
      - Enviar resultado final via SSE
      - Auto-crear campaña en BD del tenant
7. Cliente cierra conexión SSE
```

### Flujo de Cambio de Contraseña (Primer Login)
```
1. Usuario intenta acceder a /dashboard.html
2. passwordChangeMiddleware verifica must_change_password
3. Si es true:
   - Redirigir a /cambiar-password.html
   - Mostrar modal/aviso de cambio obligatorio
4. Usuario ingresa contraseña actual + nueva
5. POST /api/auth/change-password
6. AuthService.changePassword():
   a. Obtener usuario y password actual
   b. Verificar contraseña actual con bcrypt.compare()
   c. Validar longitud mínima de nueva contraseña
   d. Hashear nueva contraseña
   e. Actualizar password_hash en BD
   f. Configurar must_change_password = false
7. Usuario puede acceder al dashboard normalmente
```

---

## 🎨 INTERFACES DE USUARIO

### Rutas del Sistema

| Ruta | Página | Propósito | Acceso |
|------|--------|-----------|--------|
| `http://localhost:3000/` | Landing Page | Presentación marketing del sistema | Público |
| `http://localhost:3000/login.html` | Login | Formulario de autenticación universal | Público |
| `http://localhost:3000/dashboard.html` | Dashboard Tenant | Panel principal de validaciones | Tenant Admin |
| `http://localhost:3000/superadmin.html` | SuperAdmin | Gestión de tenants | SuperAdmin |
| `http://localhost:3000/cambiar-password.html` | Cambiar Contraseña | Cambio de contraseña obligatorio | Autenticados |

### Página Login Universal

**Ruta:** `/public/login.html`

**Características:**
- Formulario universal para SuperAdmin y Tenants
- Validación de email/password
- Guardado de token en localStorage
- Redirección automática:
  - SuperAdmin → `/superadmin.html`
  - Tenant → `/dashboard.html`
- Manejo de cambio obligatorio de contraseña
- Mensajes de error descriptivos

### Página SuperAdmin

**Ruta:** `/public/superadmin.html`

**Características:**
- **Tabs:** Tenants | Auditoría
- **Stats cards:** Total tenants, Activos, Suspendidos
- **Tabla de tenants:**
  - Nombre, Email, API Status, Estado, Último Login
  - Acciones: Suspender/Activar
  - Ver API Key (parcialmente oculta)
- **Botón "Crear Tenant"** que abre modal
- **Modal de creación:**
  - Nombre, Email, Password, API Key
  - Validación en tiempo real
  - Mensajes de error/éxito
- **Tabla de auditoría:**
  - Fecha, Usuario, Acción, Detalles (JSON)
  - Filtrable por acción
- **Logout** con redirección a login

**JavaScript:** `/public/js/superadmin.js`
- Manejo de tabs
- Llamadas a API REST
- Renderizado dinámico de tablas
- Modal de creación de tenant
- Actualización en tiempo real

### Página Dashboard Tenant

**Ruta:** `/public/dashboard.html`

**Características:**
- Header con nombre del tenant
- Secciones:
  - Validación individual
  - Validación masiva CSV
  - Historial de campañas
  - Estadísticas
- Gráficos de validaciones exitosas/fallidas
- Exportar a CSV
- Barras de progreso en tiempo real (SSE)
- Logout con redirección a login

**JavaScript:** `/public/js/dashboard.js`
- Función loadDistribuidorConfig() para mostrar nombre del tenant
- Manejo de SSE para validación masiva
- Gráficos con Chart.js o similares

### Página Cambiar Contraseña

**Ruta:** `/public/cambiar-password.html`

**Características:**
- Formulario de cambio de contraseña
- Campos: Contraseña actual, Nueva contraseña, Confirmar
- Validación de longitud mínima
- Validación de coincidencia de nueva contraseña
- Mensajes de error/éxito
- Redirección a dashboard después de cambiar
- Accesible desde el dashboard (Settings)

**JavaScript:** `/public/js/cambiar-password.js`
- Validación en tiempo real
- Llamada a POST /api/auth/change-password
- Manejo de errores
- Redirección automática

---

## 🛠️ HERRAMIENTAS DE DIAGNÓSTICO

### Scripts de Verificación (check-*.js)

```bash
# Verificar estado de la base de datos
node check-db-state.js

# Verificar estado completo de Govi
node check-govi-complete.js

# Verificar estado de Govi
node check-govi-status.js

# Verificar tenant
node check-tenant.js
```

**Funciones:**
- Conexión a PostgreSQL
- Verificar existencia de BDs
- Contar registros en tablas
- Verificar estado de tenants
- Diagnosticar problemas de datos

### Script de Diagnóstico General

```bash
node diagnostico.js
```

**Funciones:**
- Verifica conectividad a APIs de Movistar
- Valida configuración de API keys
- Muestra IP pública
- Diagnostica problemas de conexión

---

## 🚀 COMANDOS Y SCRIPTS

### En package.json

```json
{
  "scripts": {
    "dev": "npm run build:css && tsx watch src/index.ts",
    "build": "tsc",
    "build:css": "tailwindcss -i ./public/css/main.css -o ./public/css/output.css",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts",
    "test": "jest",
    "seed:superadmin": "tsx db/seed-superadmin.ts"
  }
}
```

### Uso

```bash
# Desarrollo (con hot-reload)
npm run dev

# Compilar TypeScript
npm run build

# Compilar CSS Tailwind
npm run build:css

# Producción
npm run build
npm start

# Crear SuperAdmin
npm run seed:superadmin

# Linting
npm run lint

# Tests
npm run test
```

---

## 📊 MODELOS DE DATOS

### Tenant (Usuario + Organización)

```typescript
{
  id: number;                           // Auto-incremental
  nombre: string;                       // Nombre del tenant (ej: "Govi")
  email: string;                        // Email único (ej: "govi@gmail.com")
  password_hash: string;                // Hash bcrypt (10 rounds)
  bd_name: string;                      // Nombre de BD (ej: "tenant_govi")
  api_key_encrypted: string;            // API Key cifrada (AES-256-GCM)
  api_status: 'pendiente' | 'valida' | 'invalida';
  tenant_status: 'activo' | 'suspendido';
  role: 'superadmin' | 'tenant_admin';
  must_change_password: boolean;        // true = debe cambiar en primer login
  last_login_at: Date;
  failed_login_attempts: number;        // Contador de intentos fallidos
  locked_until: Date | null;            // NULL = no bloqueado
  created_at: Date;
  updated_at: Date;
}
```

### AuditLog (Registro de Auditoría)

```typescript
{
  id: number;
  tenant_id: number;                    // ID del tenant que realizó la acción
  user_email: string;                   // Email del usuario
  action: string;                       // 'tenant_created', 'login_success', etc.
  details: JsonB;                       // Detalles adicionales (JSON flexible)
  ip_address: string;                   // IP del cliente
  created_at: Date;
}
```

### Campaign (Campaña de Validación)

```typescript
{
  id: string;                    // Formato: CMP-{timestamp}-{random}
  nombre: string;                // Ej: "Campaña 1", "Campaña 2"
  fecha: Date;                   // Fecha de creación
  tipo: 'individual' | 'multiple'; // Tipo de validación
  entorno: 'QA' | 'PROD' | 'AMBOS'; // Ambiente validado
  estadisticas: {
    totalProcesados: number;     // Total de teléfonos
    exitosos: number;            // Validaciones exitosas
    fallidos: number;            // Validaciones fallidas
    tiempoTotal: number;         // Tiempo total en segundos
  };
  resultados: ResultadoCampana[]; // Array de resultados
  creadoPor: string;             // Email del usuario
}
```

### ResultadoValidacion

```typescript
{
  telefono: string;              // 10 dígitos
  exitoso: boolean;              // true si validó correctamente
  datos?: DistribuidorResponse;  // Datos del distribuidor (si exitoso)
  error?: string;                // Mensaje de error (si falló)
  origen: 'QA' | 'PROD';         // Ambiente de validación
}
```

---

## 📝 REGLAS DE NEGOCIO

### Formato de Teléfonos
- **Longitud:** Exactamente 10 dígitos
- **Formato:** Solo números (sin espacios, guiones, paréntesis)
- **Ejemplo válido:** `9233250673`
- **Ejemplo inválido:** `923-325-0673`, `(923) 325-0673`

### Procesamiento
- **Secuencial:** No hay procesamiento paralelo
- **Delay entre ambientes:** 5 segundos
- **Delay entre teléfonos:** 5 segundos
- **Reintentos:** Hasta 3 intentos con backoff exponencial

### Ambientes
- **PROD:** Ambiente de producción principal
- **QA:** Ambiente de pruebas (si aplica)

### Gestión de Tenants
- **Nomenclatura de BD:** `tenant_{nombre_lower_sanitized}`
- **API Keys:** Validadas automáticamente antes de crear tenant
- **Cifrado:** AES-256-GCM obligatorio para API Keys
- **Aislamiento:** BD separada por tenant (máxima seguridad)

### Seguridad de Contraseñas
- **Longitud mínima:** 8 caracteres (configurable)
- **Hashing:** Bcrypt con 10 salt rounds
- **Cambio obligatorio:** Primer login
- **Intentos fallidos:** 5 antes de bloqueo (configurable)
- **Tiempo de bloqueo:** 15 minutos (configurable)

### Limitaciones del API Movistar
- **Delay de datos:** 1.5 a 2 minutos para que los datos se reflejen
- **Rate limiting:** Muy estricto, requiere delays de 5 segundos
- **Timeout:** 30 segundos por request

---

## 🎯 QUICK START (INICIO RÁPIDO)

### 1. Instalación

```bash
# Clonar repositorio (si aplica)
cd "D:\2. Proyectos 2026\DN Verification"

# Instalar dependencias
npm install
```

### 2. Configurar PostgreSQL

```sql
-- Crear base de datos principal
CREATE DATABASE bd_superadmin;

-- Conectar a bd_superadmin y ejecutar:
psql -U postgres -d bd_superadmin -f db/init.sql
```

### 3. Configurar Variables de Entorno

```bash
# Copiar template
cp .env.example .env

# Editar .env
# Generar ENCRYPTION_KEY:
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"

# Configurar:
# - ENCRYPTION_KEY (pegar la clave generada)
# - JWT_SECRET (inventar una muy larga)
# - DB_* (configuración PostgreSQL)
```

### 4. Crear SuperAdmin

```bash
# Ejecutar seed
npm run seed:superadmin

# Te pedirá:
# - Email: admin@tudominio.com
# - Password: SuperPassword123!

# IMPORTANTE: Después de crear, elimina SUPERADMIN_PASSWORD del .env
```

### 5. Iniciar Servidor

```bash
# Desarrollo (con hot-reload)
npm run dev

# El servidor iniciará en: http://localhost:3000
```

### 6. Primer Login

1. Ir a: `http://localhost:3000/login.html`
2. Email: `admin@tudominio.com`
3. Password: `SuperPassword123!`
4. Redirigirá a: `http://localhost:3000/superadmin.html`

### 7. Crear Primer Tenant

1. En SuperAdmin UI, clic en **"+ Crear Tenant"**
2. Llenar formulario:
   - Nombre: `Govi`
   - Email: `govi@gmail.com`
   - Password: `Govi12345678`
   - API Key: `sk-govi-bc62debea3abf9272efc2c91402f5d2abaffb53df26abd6c`
3. Clic en **"Crear"**
4. Debería aparecer en la lista con API Status: ✅ Válida

### 8. Login como Tenant

1. Cerrar sesión del SuperAdmin
2. Ir a: `http://localhost:3000/login.html`
3. Email: `govi@gmail.com`
4. Password: `Govi12345678`
5. **IMPORTANTE:** Te pedirá cambiar contraseña
6. Ir a: `http://localhost:3000/cambiar-password.html`
7. Cambiar a nueva contraseña
8. Redirigirá al dashboard

---

## 📖 LECTURAS OBLIGATORIAS

1. **README.md** - Documentación principal en español
2. **TESTING_GUIDE.md** - Guía completa de testing del MVP multitenant
3. **Este archivo** - Para quick reference de arquitectura
4. **examples/client-example.ts** - Ejemplos de uso del API
5. **examples/curl-examples.sh** - Ejemplos cURL

---

## ⚠️ LIMITACIONES CONOCIDAS

1. **Procesamiento secuencial exclusivo** - No se puede procesar en paralelo por limitaciones del API de Movistar

2. **Rate limiting estricto** - Delay de 5 segundos obligatorio entre requests

3. **Delay de datos** - 1.5 a 2 minutos de delay en actualizaciones del API Movistar

4. **Sin pruebas** - Jest está configurado pero no hay tests implementados

5. **Sin logging estructurado** - Usa console.log (mejorar con Winston)

6. **Sin monitoreo** - No hay métricas ni alertas (considerar Prometheus + Grafana)

7. **Sin backup automático** - No hay sistema de backups de BD

8. **Sin migraciones** - Los cambios de schema son manuales

---

## 🐛 PROBLEMAS COMUNES Y SOLUCIONES

### Error: "ENCRYPTION_KEY must be 32 bytes"
**Solución:**
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# Pegar el resultado en .env
```

### Error: "JWT_SECRET not configured"
**Solución:** Agregar `JWT_SECRET=tu_secreto_muy_largo_y_seguro` en `.env`

### Error: "Database does not exist"
**Solución:** Ejecutar `db/init.sql` en PostgreSQL

### Error: "API Key inválida" al crear tenant
**Causa:** La API Key no es válida para el endpoint de DN
**Solución:** Verificar que la API Key sea correcta y tenga acceso

### Error: "Tenant no encontrado" al validar
**Causa:** El token JWT tiene un tenant_id que no existe
**Solución:** Hacer logout y login nuevamente

### Cuenta bloqueada
**Causa:** 5 intentos fallidos de login
**Solución:** Esperar 15 minutos o que SuperAdmin reactive la cuenta

### Las validaciones no se guardan
**Solución:** Verificar que la BD del tenant existe (`tenant_{nombre}`)

---

## 🚀 PRÓXIMOS PASOS (POST-MVP)

1. **Validación periódica de API Keys** (Cron job cada 8 horas)
2. **Recuperación de contraseña** (Envío de email)
3. **Estadísticas avanzadas** por tenant
4. **Rate limiting** por IP
5. **Dashboard mejorado** para tenants
6. **Reports exportables** (CSV, PDF)
7. **WebSocket** para actualizaciones en tiempo real
8. **Backup automático** de BDs
9. **Migraciones** de base de datos
10. **Logging estructurado** con Winston
11. **Monitoreo** con Prometheus + Grafana
12. **Tests automatizados** con Jest

---

## 📞 SOPORTE

**Si surge un problema:**
1. Revisa este archivo primero
2. Revisa README.md
3. Revisa TESTING_GUIDE.md
4. Usa los scripts `check-*.js` para diagnóstico
5. Usa `diagnostico.js` para problemas de conectividad
6. Luego pregunta

---

## 🎨 LANDING PAGE DE MARKETING

### Propósito
Página web profesional de presentación orientada a ventas y marketing para mostrar el sistema a potenciales clientes.

### Ubicación
```
page/
├── index.html                    # Landing page completa
├── PRESENTACION_COTIZACION.md    # Documento de ventas
└── README.md                     # Guía de uso
```

### Acceso
- **URL Principal (Producción):** `https://liinkmobile.com/`
- **Dashboard Tenant:** `https://liinkmobile.com/dashboard.html`
- **SuperAdmin:** `https://liinkmobile.com/superadmin.html`
- **Login:** `https://liinkmobile.com/login.html`
- **Desarrollo:** `http://localhost:3000/` (local)

### Características de la Landing Page

#### Hero Section Impactante
- **Título:** "Más validaciones en menos minutos"
- **Subtítulo:** Explica el valor del sistema claramente
- **Estadísticas:** 10x más rápido, 100% automatizado, 24/7 disponible

#### Integración WhatsApp
- **Número configurado:** +52 777 335 4612
- **Mensaje predefinido:** "Hola, me interesa una demo de DN Verification"
- **Link directo:** `https://wa.me/527773354612`

---

## 🔑 CREDENCIALES DE PRODUCCIÓN

### SuperAdmin
- **URL:** https://liinkmobile.com/superadmin.html
- **Email:** admin@liinkmobile.com
- **Password:** LiinkMobile2026!
- **Rol:** SuperAdmin
- **Funciones:** Gestión de tenants, auditoría, configuración del sistema

### Ejemplo de Tenant
- **URL:** https://liinkmobile.com/dashboard.html
- **Email:** adriana.gutierrez@govicomunicaciones.net
- **Password:** password123 (temporal - cambiar en producción)
- **Rol:** Tenant Admin
- **Nombre:** GOVICOMUNICACIONES
- **Funciones:** Validación de teléfonos, campañas, estadísticas

### Notas de Seguridad
- ⚠️ **IMPORTANTE:** Cambiar las contraseñas por defecto en producción
- 🔒 Los tokens JWT tienen una validez de 24 horas
- 🛡️ El sistema bloquea cuentas después de 5 intentos fallidos (15 minutos)
- 📊 Todas las acciones críticas se registran en audit_logs

---

**Fecha:** 2026-05-22
**Versión:** 2.1.0 (Producción en liinkmobile.com)
**Estado:** 🟢 EN PRODUCCIÓN - https://liinkmobile.com

### Novedades v2.1.0:
- ✅ **Deploy en producción** en Coolify + Hetzner
- ✅ **Dominio:** https://liinkmobile.com
- ✅ **Dashboard personalizado:** Muestra nombre del tenant en el header (ej: "GOVICOMUNICACIONES")
- ✅ **Onboarding mejorado:** Tenants ya NO requieren cambio de contraseña obligatorio
- ✅ **UI simplificada:** Quitado "Sistema Multitenant" del footer del login
- ✅ **JWT con nombre del tenant:** El payload incluye el nombre para mostrarlo en el dashboard

### Novedades v2.0.0:
- ✅ **Sistema multitenant completo** con BDs separadas
- ✅ **Autenticación JWT profesional** con jsonwebtoken
- ✅ **Cifrado AES-256-GCM** para API Keys
- ✅ **Roles:** SuperAdmin y Tenant Admin
- ✅ **Sistema de auditoría completo**
- ✅ **Cambio de contraseña** opcional (no obligatorio para tenants)
- ✅ **Bloqueo de cuentas** por intentos fallidos
- ✅ **UI SuperAdmin** para gestión de tenants
- ✅ **Validación automática de API Keys**
- ✅ **Middleware de autenticación y roles**
- ✅ **UI de cambio de contraseña**
- ✅ **Scripts de diagnóstico y verificación**
- ✅ **PostgreSQL** como base de datos principal
- ✅ **Bcrypt** para hashing de contraseñas
- ✅ **Landing page de marketing** con integración WhatsApp

---

## 🚢 DESPLIEGUE EN PRODUCCIÓN (COOLIFY + HETZNER)

### Infraestructura de Producción

**Proveedor:** Hetzner
**Plataforma:** Coolify
**Dominio:** https://liinkmobile.com/
**Base de Datos:** PostgreSQL gestionado por Coolify

---

### 1. CONFIGURACIÓN DEL SERVIDOR HETZNER

#### Requisitos del Servidor
- **SO:** Ubuntu 22.04 LTS o superior
- **RAM:** Mínimo 4GB (recomendado 8GB)
- **CPU:** Mínimo 2 cores (recomendado 4 cores)
- **Almacenamiento:** Mínimo 80GB SSD
- **Red:** IPv4 + IPv6

#### Pasos Iniciales en Hetzner

1. **Crear el servidor (Cloud Server):**
   - Ir a Hetzner Cloud Console
   - Crear nuevo proyecto: "liinkmobile-dn-verification"
   - Crear servidor: Ubuntu 22.04, CX41 o superior
   - Agregar SSH key para acceso
   - Configurar firewall:
     - Puertos permitidos: 22 (SSH), 80 (HTTP), 443 (HTTPS)
     - Origen: 0.0.0.0/0 para 80/443, tu IP para 22

2. **Instalar Docker:**
   ```bash
   # Conectar al servidor via SSH
   ssh root@tu_ip_hetzner

   # Instalar Docker
   curl -sSL https://get.docker.com | sh

   # Verificar instalación
   docker --version
   ```

3. **Instalar Coolify:**
   ```bash
   # Instalar Coolify con un comando
   curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash

   # Esperar instalación (5-10 minutos)
   # Coolify se instalará en puerto 8000
   ```

4. **Configurar DNS:**
   - Ir al proveedor de dominios (donde compraste liinkmobile.com)
   - Agregar registro A:
     - Nombre: `@` → IP del servidor Hetzner
     - Nombre: `*` → IP del servidor Hetzner (wildcard)
   - Configurar CNAME para www (opcional):
     - Nombre: `www` → `liinkmobile.com`

---

### 2. CONFIGURACIÓN DE COOLIFY

#### Acceso Inicial
1. Abrir: `http://tu_ip_hetzner:8000`
2. Crear cuenta de administrador
3. Configurar dominio principal: `liinkmobile.com`
4. Coolify configurará automáticamente SSL con Let's Encrypt

#### Crear Proyecto en Coolify
1. **Nombre del proyecto:** `dn-verification`
2. **Descripción:** Sistema de validación DN para Movistar

#### Agregar Base de Datos PostgreSQL
1. En el proyecto, ir a "Database" → "New Database"
2. **Tipo:** PostgreSQL 16
3. **Nombre:** `dn-verification-db`
4. **Versión:** 16 (última estable)
5. **Configuración:**
   - RAM: 2GB
   - Storage: 50GB
6. Coolify generará automáticamente:
   - `DB_HOST` (internal)
   - `DB_PORT` (5432)
   - `DB_NAME` (dn_verification_db)
   - `DB_USER` (postgres)
   - `DB_PASSWORD` (autogenerado)

---

### 3. PREPARAR REPOSITORIO GIT

#### Subir Código a GitHub/GitLab
```bash
cd "D:\2. Proyectos 2026\DN Verification"

# Inicializar git (si no está inicializado)
git init

# Agregar .gitignore si no existe
cat > .gitignore << 'EOF'
node_modules/
dist/
.env
uploads/*.csv
.DS_Store
*.log
.vscode/
.idea/
EOF

# Commit inicial
git add .
git commit -m "Initial commit: DN Verification System v2.0.0"

# Agregar remote
git remote add origin https://github.com/tu-usuario/dn-verification.git

# Push
git branch -M main
git push -u origin main
```

---

### 4. CONFIGURAR APLICACIÓN EN COOLIFY

#### Crear Nueva Aplicación
1. En Coolify: `New Resource` → `Application` → `From Git`
2. **Conectar repositorio:** GitHub/GitLab
3. **Seleccionar branch:** `main` (o `production`)
4. **Configurar build:**
   - **Build Pack:** Node.js
   - **Node Version:** 20
   - **Install Command:** `npm install`
   - **Build Command:** `npm run build && npm run build:css`
   - **Start Command:** `npm start`
   - **Port:** 3000

#### Variables de Entorno de Producción
```bash
# ============================================
# SERVIDOR
# ============================================
PORT=3000
NODE_ENV=production

# ============================================
# BASE DE DATOS (Generadas por Coolify)
# ============================================
DB_HOST=postgres_service_internal  # Servicio interno de Coolify
DB_PORT=5432
DB_USER=postgres_user_coolify      # Generado por Coolify
DB_PASSWORD=db_password_coolify    # Generado por Coolify
DB_NAME=dn_verification_db         # Nombre de BD en Coolify

# ============================================
# CIFRADO DE API KEYS - GENERAR NUEVO
# ============================================
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=tu_clave_base64_44_caracteres_PROD_aqui

# ============================================
# JWT - GENERAR NUEVO SECRETO
# ============================================
# Generar con: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=tu_jwt_secreto_PROD_muy_largo_y_seguro_aqui
JWT_EXPIRATION=24h

# ============================================
# VALIDACIÓN DE API KEYS
# ============================================
VALIDATION_TEST_PHONE=9233250673

# ============================================
# SEGURIDAD DE CONTRASEÑAS
# ============================================
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCK_TIME=900
PASSWORD_MIN_LENGTH=8

# ============================================
# COOLIFY ESPECÍFICO
# ============================================
# URL de la aplicación
APP_URL=https://liinkmobile.com
```

**IMPORTANTE:** Nunca compartir estas variables de producción. Guardarlas en lugar seguro.

---

### 5. EJECUTAR MIGRACIONES EN PRODUCCIÓN

#### Opción A: Terminal de Coolify
1. Ir a la aplicación en Coolify
2. Clic en "Terminal"
3. Ejecutar comandos:
   ```bash
   # Ejecutar script de inicialización de BD
   cat db/init.sql | psql $DB_CONNECTION_STRING

   # Crear SuperAdmin
   npm run seed:superadmin
   ```

#### Opción B: One-time Script
1. Crear script `migrate-prod.js`:
   ```javascript
   const { Pool } = require('pg');
   const fs = require('fs');
   const bcrypt = require('bcrypt');

   const pool = new Pool({
     connectionString: process.env.DATABASE_URL
   });

   async function migrate() {
     const client = await pool.connect();
     try {
       // Ejecutar init.sql
       const initSql = fs.readFileSync('db/init.sql', 'utf8');
       await client.query(initSql);
       console.log('✅ Schema creado');

       // Crear SuperAdmin
       const email = process.env.SUPERADMIN_EMAIL;
       const password = process.env.SUPERADMIN_PASSWORD;
       const hashedPassword = await bcrypt.hash(password, 10);

       await client.query(
         `INSERT INTO tenants (nombre, email, password_hash, bd_name, role, tenant_status, must_change_password)
          VALUES ($1, $2, $3, $4, $5, $6, $7)`,
         ['SuperAdmin', email, hashedPassword, 'bd_superadmin', 'superadmin', 'activo', false]
       );
       console.log('✅ SuperAdmin creado');
     } finally {
       client.release();
       await pool.end();
     }
   }

   migrate().catch(console.error);
   ```

2. En Coolify, agregar variables temporales:
   - `SUPERADMIN_EMAIL`: admin@liinkmobile.com
   - `SUPERADMIN_PASSWORD`: (contraseña temporal)

3. Ejecutar script y luego eliminar variables temporales

---

### 6. CONFIGURAR DOMINIO Y SSL

#### Dominio Principal
1. En Coolify, ir a "Domains"
2. Agregar dominio: `liinkmobile.com`
3. Coolify configurará automáticamente:
   - Certificado SSL (Let's Encrypt)
   - Reverse Proxy (Nginx/Traefik)
   - HTTP → HTTPS redirect

#### Dominios Adicionales (Opcional)
- `api.liinkmobile.com` → Para API (si se quiere separar)
- `app.liinkmobile.com` → Para aplicación web
- `www.liinkmobile.com` → Redirect a liinkmobile.com

#### Verificar SSL
1. Esperar 5-10 minutos después de agregar dominio
2. Verificar con: `curl -I https://liinkmobile.com`
3. Debe mostrar: `HTTP/2 200` con headers de SSL

---

### 7. ACTUALIZAR URLs EN EL SISTEMA

#### Cambiar URLs en Código

**1. Actualizar landing page (`page/index.html`):**
```html
<!-- Cambiar localhost por dominio de producción -->
<a href="https://liinkmobile.com/login.html" class="btn-primary">
  Iniciar Sesión
</a>
```

**2. Actualizar configuración de CORS (`src/index.ts`):**
```typescript
app.use(cors({
  origin: [
    'https://liinkmobile.com',
    'https://www.liinkmobile.com',
    'http://localhost:3000' // Desarrollo
  ],
  credentials: true
}));
```

**3. Actualizar documentación:**
- Actualizar este archivo con URLs de producción
- Actualizar README.md
- Actualizar TESTING_GUIDE.md

#### URLs de Producción

| Servicio | URL Producción |
|----------|----------------|
| Landing Page | `https://liinkmobile.com/` |
| Login | `https://liinkmobile.com/login.html` |
| Dashboard Tenant | `https://liinkmobile.com/dashboard.html` |
| SuperAdmin | `https://liinkmobile.com/superadmin.html` |
| API Base | `https://liinkmobile.com/api` |
| API Auth | `https://liinkmobile.com/api/auth` |
| API Validate | `https://liinkmobile.com/api/validate` |

---

### 8. CONFIGURAR BACKUPS AUTOMÁTICOS

#### Backup de Base de Datos en Coolify
1. Ir a la base de datos en Coolify
2. Activar "Automatic Backups"
3. **Configuración:**
   - **Frecuencia:** Diaria (3:00 AM)
   - **Retención:** 30 días
   - **Destino:** S3 compatible (Hetzner Storage Box o Wasabi)

#### Backup de Archivos
```bash
# Crear script de backup (backup-files.sh)
#!/bin/bash
# Backup de uploads (archivos CSV temporales)
tar -czf uploads-backup-$(date +%Y%m%d).tar.gz uploads/
# Subir a almacenamiento externo
rclone copy uploads-backup-*.tar.gz s3:liinkmobile-backups/
```

---

### 9. MONITOREO Y LOGGING

#### Configurar Alertas en Coolify
1. Ir a "Notifications"
2. Configurar alertas:
   - **Discord:** Webhook para canal de operaciones
   - **Email:** admin@liinkmobile.com
   - **Slack:** Webhook (opcional)

#### Métricas a Monitorear
- CPU Usage > 80%
- Memory Usage > 85%
- Disk Space < 20%
- Response Time > 5s
- Error Rate > 5%

#### Logs
```bash
# Ver logs en tiempo real
docker logs -f coolify-container-id

# Ver logs de la aplicación
docker logs -f dn-verification-app

# Exportar logs
docker logs dn-verification-app > logs-$(date +%Y%m%d).log
```

---

### 10. SEGURIDAD EN PRODUCCIÓN

#### Configuraciones Adicionales

**1. Firewall Adicional:**
```bash
# Permitir solo tráfico necesario
ufw allow 22    # SSH
ufw allow 80    # HTTP
ufw allow 443   # HTTPS
ufw enable
```

**2. Rate Limiting (Nginx/Caddy):**
```nginx
# Limitar requests por IP
limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;

location /api/ {
    limit_req zone=api burst=20 nodelay;
    # ...
}
```

**3. Headers de Seguridad:**
```typescript
// En src/index.ts
import helmet from 'helmet';
app.use(helmet({
  hsts: {
    maxAge: 31536000,
    includeSubDomains: true,
    preload: true
  },
  contentSecurityPolicy: {
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", "data:", "https:"],
    }
  }
}));
```

**4. Variables de Entorno Seguras:**
- Nunca commitear `.env`
- Rotar contraseñas mensualmente
- Usar secrets de Coolify (no en archivo .env)

---

### 11. CI/CD CON COOLIFY

#### Configurar Deploy Automático

**Opción A: Auto-deploy en Push**
1. En Coolify, activar "Auto-deploy"
2. Cada push a `main` activará deploy
3. Coolify ejecutará: `npm install → npm run build → npm start`

**Opción B: Deploy Manual**
1. Hacer push y cambios
2. En Coolify, clic "Deploy"
3. Seguir logs en tiempo real

#### Workflow de Deploy
```bash
# 1. Desarrollo local
git checkout -b feature/nueva-funcionalidad
# ... hacer cambios ...
git commit -am "Add nueva funcionalidad"

# 2. Testing
npm run test
npm run lint

# 3. Merge a main
git checkout main
git merge feature/nueva-funcionalidad

# 4. Push y deploy
git push origin main
# Coolify detectará y hará deploy automático
```

---

### 12. POST-DEPLOY CHECKLIST

#### Inmediatamente Después del Deploy
- [ ] Verificar que el servidor responde: `curl https://liinkmobile.com`
- [ ] Verificar health check: `curl https://liinkmobile.com/api/health`
- [ ] Login como SuperAdmin
- [ ] Verificar que se puede crear un tenant
- [ ] Probar validación de teléfono
- [ ] Verificar que los CSVs se procesan correctamente
- [ ] Verificar logs no muestran errores

#### Verificación de Seguridad
- [ ] HTTPS funciona correctamente
- [ ] Certificado SSL es válido
- [ ] HTTP redirige a HTTPS
- [ ] CORS está configurado correctamente
- [ ] Variables de entorno son seguras
- [ ] No hay datos en logs sensibles

#### Verificación de Funcionalidad
- [ ] Landing page carga correctamente
- [ ] Login funciona (SuperAdmin y Tenant)
- [ ] Creación de tenants funciona
- [ ] Validación individual funciona
- [ ] Validación masiva CSV funciona
- [ ] Auditoría registra eventos
- [ ] Cambio de contraseña funciona

---

### 13. SOLUCIÓN DE PROBLEMAS EN PRODUCCIÓN

#### Error: "Cannot connect to database"
**Solución:**
1. Verificar que la base de datos está running en Coolify
2. Verificar variables de entorno (`DB_HOST`, `DB_PORT`, etc.)
3. Verificar red interna de Coolify
4. Revisar logs: `docker logs <db-container-id>`

#### Error: "SSL Certificate Invalid"
**Solución:**
1. Esperar 10-15 minutos después de configurar dominio
2. Verificar que DNS apunta correctamente: `dig liinkmobile.com`
3. Forzar renovación en Coolify: "Domains" → "Renew Certificate"
4. Verificar puerto 443 está abierto en firewall

#### Error: "Application not responding"
**Solución:**
1. Verificar logs de aplicación
2. Reiniciar aplicación en Coolify
3. Verificar uso de recursos (CPU/RAM)
4. Escalar servidor si es necesario

#### Error: "Upload CSV fails"
**Solución:**
1. Verificar permisos de carpeta `uploads/`
2. Verificar espacio en disco
3. Aumentar `client_max_body_size` en Nginx/Caddy
4. Verificar configuración de Multer

---

### 14. ACTUALIZAR DOCUMENTACIÓN CON URLs DE PRODUCCIÓN

#### Cambiar Referencias de localhost → liinkmobile.com

**En este archivo (.contenido_sistema.md):**
- Línea 1190: `http://localhost:3000/` → `https://liinkmobile.com/`
- Línea 1191: `http://localhost:3000/dashboard.html` → `https://liinkmobile.com/dashboard.html`
- Línea 1192: `http://localhost:3000/superadmin.html` → `https://liinkmobile.com/superadmin.html`
- Línea 1193: `http://localhost:3000/login.html` → `https://liinkmobile.com/login.html`

**En README.md:**
- Actualizar todos los ejemplos de cURL con URLs de producción
- Actualizar instrucciones de acceso
- Agregar nota sobre entorno de producción

---

## 📊 TABLA DE ENTORNOS

| Entorno | URL | Base de Datos | Propósito | Estado |
|---------|-----|---------------|-----------|--------|
| **Desarrollo** | `http://localhost:3000` | `bd_superadmin` (local) | Desarrollo local | 🟡 Local |
| **Staging** | `https://staging.liinkmobile.com` | `dn_verification_staging` | Testing pre-producción | ⚪ Configurar |
| **Producción** | `https://liinkmobile.com` | `bd_superadmin` (Coolify) | Sistema en vivo | 🟢 Activo |

### Bases de Datos en Producción
- **bd_superadmin:** Base de datos principal (usuarios, tenants, auditoría)
- **tenant_govicomunicaciones:** Base de datos del tenant Govicomunicaciones
- **tenant_{nombre}:** Una base de datos separada por tenant

---

## 🎯 CHECKLIST FINAL DE PRODUCCIÓN

### Pre-Deploy ✅ Completado
- [x] Todo el código está commiteado en `main`
- [x] Build funciona (`npm run build`)
- [x] Variables de entorno están configuradas en Coolify
- [x] Dominio liinkmobile.com apunta a IP correcta
- [x] Base de datos bd_superadmin está creada en Coolify
- [x] Landing page movida a public/ para acceso correcto

### Post-Deploy ✅ Completado
- [x] Sitio es accesible via HTTPS
- [x] Login funciona (SuperAdmin y Tenants)
- [x] API responde correctamente
- [x] SuperAdmin creado (admin@liinkmobile.com)
- [x] Tenant de prueba creado (GOVICOMUNICACIONES)
- [x] Dashboard muestra nombre del tenant en header
- [x] No hay errores de conexión a base de datos
- [x] Documentación actualizada

### Pendientes ⚠️
- [ ] Tests automatizados (Jest configurado pero sin tests)
- [ ] Cambiar contraseñas por defecto en producción
- [ ] Configurar backups automáticos de base de datos
- [ ] Configurar monitoreo y alertas
- [ ] Configurar rate limiting por IP

---

**FIN DE LA DOCUMENTACIÓN**
